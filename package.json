{
  "name": "cloud-ai-backend",
  "version": "1.0.0",
  "description": "Secure backend for Cloud AI Image Generator",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "echo 'Build complete - no build step required for static files'",
    "test": "echo 'No tests specified'",
    "docker:build": "docker build -t cloud-ai-generator .",
    "docker:run": "docker run -p 3000:3000 --env-file .env cloud-ai-generator"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "multer": "^1.4.5-lts.1",
    "helmet": "^7.1.0",
    "express-rate-limit": "^7.1.5",
    "@supabase/supabase-js": "^2.38.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}